{
    "name": "pg-tenant-api",
    "version": "1.45.0",
    "description": "Serverless CRUD service exposing a REST HTTP interface",
    "author": "",
    "license": "MIT",
    "scripts": {
        "start": "sls offline start",
        "dynamodb-admin": "AWS_SECRET_ACCESS_KEY=fake AWS_ACCESS_KEY_ID=fake AWS_REGION=local DYNAMO_ENDPOINT=http://localhost:8000 dynamodb-admin",
        "migrate": "NODE_ENV=local node ./migrations --up",
        "migrate:down": "NODE_ENV=local node ./migrations --down",
        "test:e2e": "node_modules/.bin/mocha e2e-test/* --recursive --exit "
    },
    "dependencies": {
        "@dhteam/pg-http-kit": "^1.0.0",
        "@dhteam/pg-serverless-kit": "^1.0.2-dev.2",
        "@dhteam/pg-swagger-kit": "^1.0.3",
        "amazon-dax-client": "^1.2.5",
        "aws-sdk": "^2.826.0",
        "uuid": "^2.0.3",
        "yamljs": "^0.3.0"
    },
    "devDependencies": {
        "chai": "^4.2.0",
        "mocha": "^8.2.1",
        "serverless-offline": "^6.8.0",
        "supertest": "^6.1.3"
    }
}
